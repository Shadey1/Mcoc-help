// The season the war planner serves. Rewritten by
// `scripts/extract-aw-season.ts --apply` when a newer season lands.
import season from './season-69.json' with { type: 'json' };

export default season;
