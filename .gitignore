node_modules
.next
out
dist
*.log
.DS_Store
.env*.local
.turbo
coverage
.cache
*.tsbuildinfo

# GuiaMTC page dumps for AW season extraction — not committed (large,
# regeneratable by saving the page from the browser and dropping here)
/dump/
/Dump/

# Season extraction review artefacts — regenerated by the extractor
/data/aw/_cells-s*/

# Reference portrait PNGs for the AW extractor — regenerable via
# pnpm fetch-portraits, ~5 MB total, kept out of the repo.
/data/champions/portraits-cache/

# Written by scripts/auto-refresh.ts for the workflow to read
/auto-refresh-report.md
