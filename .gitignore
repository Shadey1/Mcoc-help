node_modules
.next
out
dist
*.log
.DS_Store
.env*.local
.turbo
coverage
.cache
*.tsbuildinfo

# GuiaMTC page dumps for AW season extraction — not committed (large,
# regeneratable by saving the page from the browser and dropping here)
/dump/
/Dump/

# Season extraction review artefacts — regenerated by the extractor
/data/aw/_review-s*/
/data/aw/_cells-s*/

# Raw portrait PNG cache — regenerable via pnpm build-portrait-hashes,
# ~5 MB total, kept out of the repo. The phash JSON stays committed.
/data/champions/portraits-cache/
